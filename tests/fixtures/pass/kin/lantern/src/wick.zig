// Nothing outside the language, so the workspace's shared `use ledger` is dormant here
// — dormant, not stale: `hearth` exercises it, and one member cannot see the other.
const _0 = @import("std");
