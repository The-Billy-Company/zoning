// The inherited grant, exercised. The same grant is dormant in `lantern`, which is
// why a shared line cannot be judged one member at a time.
const _0 = @import("ledger");
const _1 = @import("std");
